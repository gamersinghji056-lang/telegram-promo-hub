import { createFileRoute } from "@tanstack/react-router"; import { PublicPage } from "@/components/public-site"; import { publicPageHead } from "@/lib/public-meta";
export const Route = createFileRoute("/mark")({ head:()=>publicPageHead("MARK — Coming Soon","Meet MARK, an intelligent Telegram assistant being designed around business knowledge, instructions and context.","/mark"), component:()=> <PublicPage page="mark"/> });
